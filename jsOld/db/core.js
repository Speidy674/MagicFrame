const path = require('path');
const fs = require('fs');


function Database(config) {
    console.log("Start Database Core")
    console.debug("Database not impl")


    this.sync = async () => {
        console.debug("Datenbank not impl")
    };
}

module.exports = Database